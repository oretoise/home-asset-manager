
services:
  database:
    image: 'postgres:18'
    ports:
      - 5432:5432

    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DATABASE

    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: